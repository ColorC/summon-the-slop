# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri build output
**/target/
**/target-check/
src-tauri/gen
**/.omni/
dist-web

# 笔记数据(运行态): AI 块工作目录 + 笔记落盘库都在仓外, 但 ai-blocks 会建在仓内 → 忽略
ai-blocks/

# Vendored external tools (pulled down for reference / integration, not uploaded)
vendor/
# ...but DO track vendored runtime libs under public/ (rrweb for 录像/回放, served at app://)
!public/vendor/
# ...and the Chrome extension's bundled rrweb (P2 — must ship in the extension, no CDN)
!chrome-extension/vendor/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# local launcher (machine-specific node path) — not for git
start-poof.cmd
start-poof.sh

# notes UI 离屏验证截图(测试产物)
preview-*.png
