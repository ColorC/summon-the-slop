// Dashboard imports this entry directly into its own document. The implementation
// stays in notes-web.tsx so the standalone page and embedded sidebar cannot drift.
export { mountMaterialNotesWorkspace } from "./notes-web";
