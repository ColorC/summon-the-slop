{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recbar",
  "description": "Capability for the 录制条 (recording control bar): listen for rec-started/rec-stopped and show/hide itself + the stop button invokes region_record_stop. 本文件只约束 Tauri core 窗口/事件 API 的可达性,不是自定义命令防火墙——所有 #[tauri::command] 对任何窗口开放;未来若有窗口要渲染不受信内容,须先把 run_shell/ask_ai 等高危命令声明成具名权限再按窗口分配。",
  "windows": ["recbar"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging"
  ]
}
