{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "snap",
  "description": "Capability for the 截图 (snap) window: listen for snap-summon and show/hide/focus itself. Without core:event the snap-summon listener is never registered and clicking 相机 does nothing.",
  "windows": ["snap"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus"
  ]
}
